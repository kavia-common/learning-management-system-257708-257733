# Create React App only exposes variables prefixed with REACT_APP_
# Copy this file to .env and set the values before running the app.

# PUBLIC_INTERFACE
# Supabase project URL (e.g., https://xyzcompany.supabase.co)
REACT_APP_SUPABASE_URL=

# PUBLIC_INTERFACE
# Supabase anon (public) API key
# Prefer this name:
REACT_APP_SUPABASE_ANON_KEY=
# If your environment only provides REACT_APP_SUPABASE_KEY, map it to the above in your CI/CD,
# or set both variables with the same value.

# PUBLIC_INTERFACE
# The fully-qualified frontend URL used for Supabase email redirect after signup
# Example for local dev with CRA:
# http://localhost:3000  (or the actual port in your environment)
REACT_APP_FRONTEND_URL=
