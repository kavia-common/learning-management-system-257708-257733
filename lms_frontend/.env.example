# Frontend build configuration for Supabase (used at build time by CRA)
# Do NOT commit real secrets. Copy this file to .env and fill values locally or set in CI/CD.

# Required: Supabase project URL (e.g., https://xyzcompany.supabase.co)
SUPABASE_URL=

# Required: Supabase public key (client-safe). We do not use a separate "anon" variable name.
SUPABASE_KEY=

# Optional: Frontend URL used for Supabase email redirect on signup (defaults to window.location.origin)
REACT_APP_FRONTEND_URL=
