# Example environment variables for the LMS frontend (Create React App)

# Primary (preferred) variables - CRA exposes only REACT_APP_* to the browser
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOi...

# Optional convenience mapping if your CI/CD provides unprefixed names:
# Note: CRA does NOT expose these to the browser unless mapped to REACT_APP_*.
# They are here to document expected names in some secret stores.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=eyJhbGciOi...

# Other app variables (examples from project)
REACT_APP_FRONTEND_URL=http://localhost:3000

# Dev notes:
# - Do NOT commit real keys. Use this file as a reference only.
# - In CI/CD, export REACT_APP_SUPABASE_URL and REACT_APP_SUPABASE_ANON_KEY before building.
